{
  "name": "homelab-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Discord bot for the kuipr homelab",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../.env --env-file-if-exists=.env src/index.ts",
    "dev:register": "node --env-file-if-exists=../../.env --env-file-if-exists=.env src/register-commands.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "discord.js": "^14.16.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.6.3"
  }
}
